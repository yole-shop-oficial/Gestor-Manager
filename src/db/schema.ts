// Schema placeholder: local PostgreSQL is not used
// as the primary business database for YOLE SHOP APP.
// All core business data (auth, pedidos, wallet, comisiones)
// lives in Supabase projects. This file stays minimal so
// Drizzle can still initialize for health checks.

export {};