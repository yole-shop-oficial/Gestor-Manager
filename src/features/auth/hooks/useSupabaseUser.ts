"use client";

/**
 * @deprecated Use `useSession` from `@/hooks` instead.
 * This is a backward-compatible alias that will be removed in a future version.
 */
export { useSession as useSupabaseUser } from "@/hooks/useSession";
